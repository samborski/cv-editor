{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Copiar src a dist",
            "type": "shell",
            "command": "rm -rf dist && mkdir dist && cp -r src/* dist/",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
